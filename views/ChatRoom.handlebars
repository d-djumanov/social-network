<div class="container-fluid">
   <div style="margin-top:80px; margin-right: 14px; background-color: antiquewhite; flex-direction:  row; display: flex; justify-content: space-between;">
    <h2 style="text-align: left;">{{otherUser.username}}</h2>
    <h2 style="text-align: right;">{{currentUser.username}}</h2>
   </div>
   <hr style="color:black; margin: 0px;">

    <input type="hidden" id="otherUserId" value="{{otherUser._id}}" >
    <input type="hidden" id="currentUserId" value="{{currentUserId}}" >

    <div id="chatRoom" style="margin: 0px;"></div>

    <div>
        <form id="form">
        <input type="text" name="message" id="message" placeholder="Type message .." required>
        <button id="send-btn" type="submit">Send</button>
        </form>
    </div>

    
</div>